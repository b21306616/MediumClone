# Routes folder

"routes\web.php" is a file inside which we are going to define all the routes and we access those routes in a browser.

"routes\console.php" is for writing console commands.
As an example, the "inspire" command exists in this file.
When you run "php artisan inspire" in your terminal, it displays a random inspiring quote from a famous programmer, philosopher, or scientist.
