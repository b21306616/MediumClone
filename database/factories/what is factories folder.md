# factories folder

Purpose of the factory is actually to define the definition for each field.
So that whenever we generate some data for that user as an example, it will use that fake data to generate the data.

For example: The "UserFactory.php" file defines the default blueprint (fallback values) for all user fields.
