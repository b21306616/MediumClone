# Models

You can think of a model as an entity in the application.

## Useful CLI Commands

```php
# Create a model named Category
php artisan make:model Category

# Create a model named Category with migration
php artisan make:model Category -m # -m creates a migration file for the model.

# To get information about the make:model command
php artisan make:model --help
```
