# Seeder files

The seeder files are used to seed the database with dummy data.  
The `database\seeders\DatabaseSeeder.php` file is the entry point for all the seeder files. This file is executed when you run `php artisan db:seed` command.
